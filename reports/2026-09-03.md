# tokenloom eval 2026-09-03 / model opus / prompt 57dcdf29f1ed / mcp captures 0/none

모든 숫자는 `runs/*.jsonl`의 실제 호출 줄에서 집계했다. fake 실행은 제외한다.

## 되는 것

| 입력 | IR | S1 | S2 | S3 | 입력토큰 p50 | 지연 p50 |
| --- | --- | --- | --- | --- | --- | --- |
| snapshot | compact | 0.00 | 1.00 | n/a | 5346 | 19715 |
| snapshot | compact+annotations | 0.00 | 1.00 | n/a | 5413 | 22075 |
| snapshot | raw | 0.00 | 0.90 | n/a | 47875 | 25568 |

## 안 되는 것 (다음 버전 대기)

| 항목 | 실패 양상 | 재시도 조건 |
| --- | --- | --- |
| button/raw | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| button/compact | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| button/compact+annotations | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| icon-button/compact | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| icon-button/compact+annotations | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| twenty-variants/compact | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| twenty-variants/compact+annotations | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| icon-button/raw | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| twenty-variants/raw | S1 0.00 below threshold 0.90 (a var(--x) is absent from tokens.css) | IR names every token sub-property the CSS needs |
| icon-button/raw | S2 0.71 below threshold 0.95 | prompt or emitter changes |

## 입력 경로별 고정 비용

| 입력 | 세션 스키마 토큰 | 컴포넌트당 토큰 p50 |
| --- | --- | --- |
| snapshot | 11019 | 6185 |

## 표본 크기와 한계

- combinations: 9
- sent: 9
- skipped: 0
- total cost usd: 2.2986
- output tokens p50: 2139
