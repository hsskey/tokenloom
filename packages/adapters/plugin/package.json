{
  "name": "@tokenloom/plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && esbuild src/code.ts --bundle --outfile=${TOKENLOOM_PLUGIN_OUTFILE:-dist/code.js} --format=iife --target=es2017 --define:__TL_EXPORTER_SHA__=\"'$(git rev-parse HEAD)$(git status --porcelain | grep -q . && printf -- -dirty)'\" --define:__TL_EXPORTER_BUILT_AT__=\"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@tokenloom/schema": "workspace:*"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "esbuild": "^0.24.2"
  }
}
